language: node_js
node_js:
  - stable

jobs:
  include:
    - stage: npm release
      node_js: stable
      script: echo "Deploying to npm ..."
      deploy:
        provider: npm
        api_key: $NPM_API_KEY
        on:
          branch: master
